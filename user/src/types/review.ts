export type UserReview = {
  uuid: string;
  name: string;
};

export type Review = {
  id: number;
  name: string;
  avatarUrl: string;
  rating: number;
  comment: string;
  createdAt: string;
  updatedAt: string;
};
